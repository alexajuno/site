# Package manager

Always use `bun` for commands in this project — not `npm`, `yarn`, or `pnpm`.

- Install dependencies: `bun install`
- Run scripts: `bun run <script>` (or `bun <script>` for scripts defined in `package.json`)
- Add a dependency: `bun add <package>`
- Execute a package binary: `bunx <package>`
