# Commit message style

Keep commit message subjects short, lowercase, and imperative — no trailing period, no conventional-commits prefix (`feat:`, `fix:`, etc.), no scope.

Examples from this repo's history:

- `add shadcn ui setup`
- `add blog content collection schema`
- `add package manager claude rule`
