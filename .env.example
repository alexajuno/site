# Database - Choose one:

# Option 1: Local PostgreSQL with Docker (docker compose up -d)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/site?schema=public"

# Option 2: Cloud database (Neon, Supabase, Railway, etc.)
# DATABASE_URL="postgres://username:password@hostname/database?sslmode=require"
